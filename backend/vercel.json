{
    "version": 2,
    "installCommand": "pnpm install",
    "builds": [
        {
            "src": "index.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "index.js"
        }
    ],
    "ignoreCommand": "git diff HEAD^ HEAD --quiet -- ./"
}

